@page
@model IndexModel
@{
    ViewData["Title"] = "HALFFLUX Studios";
}

<div class="text-center">
    <h1 class="display-4 text-white font-barlow">We make games! Join us!</h1>
    <a value="Press" asp-controller="Changelog" asp-action="OnButtonPress" type="submit" class="btn btn-success" id="button"></a>
</div>